# wireless_optim
```
pip install dm-haiku -q
pip install optax -q
git clone https://github.com/luchris429/purejaxrl.git -q
pip install RLax -q

pip install -e .

pip install --upgrade jax jaxlib

pip install -U "jax[cuda12]"
```